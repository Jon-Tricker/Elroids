// A group of component sets, for example, a shopping catalogue.

class ComponentSets extends Set {

    constructor() {
        super();
    }

    add(compSet) {
        super.add(compSet);
    } 
    
    delete(compSet) {
        super.delete(compSet)
    }
}

export default ComponentSets;